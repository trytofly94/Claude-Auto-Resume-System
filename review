#!/usr/bin/env bash

# Claude Auto-Resume - Review Command Wrapper
# Provides /review command functionality for the agent workflow system
# Integrates with existing task queue and monitoring infrastructure

set -euo pipefail

# Determine script location
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Execute the PR review script with all arguments
exec "$SCRIPT_DIR/src/pr-review.sh" "$@"